name: server
description: 驴看板的局域网同步服务端。单文件可执行，托盘常驻。
version: 0.3.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shared:
    path: ../shared
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  sqlite3: ^3.5.1
  args: ^2.6.0
  path: ^1.9.0
  qr: ^4.0.0
  archive: ^4.0.9
  crypto: ^3.0.7

dev_dependencies:
  async: ^2.13.1
  lints: ^6.0.0
  test: ^1.25.6
