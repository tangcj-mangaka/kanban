name: shared
description: 驴看板的共享层——数据模型、op log 定义与同步协议，客户端与服务端共用。
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
